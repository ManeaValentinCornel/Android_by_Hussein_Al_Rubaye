fun main(){

    var car=Car("Bmw",2020,"Green")

    car.name
    //car.age
    //cannot be accessed because the car is an private attribute
    //car.color
    //cannot be accessed because is protected
}